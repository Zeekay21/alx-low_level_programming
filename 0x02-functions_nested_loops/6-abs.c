#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return: 0
 */
int main(void)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
